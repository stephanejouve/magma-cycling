# Ruff configuration for magma-cycling

# Line length (match black's default)
line-length = 120

[lint]
# Ignore specific rules
ignore = [
    "B007",  # Loop control variable not used (false positive)
    "E501",  # Line too long (handled by black)
]
